from django.urls import path
# from . import views

app_name = 'onboarding'

urlpatterns = [
    # TODO: Implement onboarding API endpoints
    # path('company-search/', views.CompanySearchView.as_view(), name='company_search'),
    # path('company-profile/', views.CompanyProfileView.as_view(), name='company_profile'),
    # Add endpoints as views are implemented
]
